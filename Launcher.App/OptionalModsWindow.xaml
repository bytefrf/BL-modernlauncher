<Window x:Class="Launcher.App.OptionalModsWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Дополнительные моды"
        Icon="/Sprite-0001.png"
        Width="760"
        Height="620"
        WindowStyle="None"
        ResizeMode="NoResize"
        WindowStartupLocation="CenterOwner"
        Background="{DynamicResource WindowBackgroundBrush}">
    <Window.Resources>
        <Style x:Key="FlatButton" TargetType="Button">
            <Setter Property="Height" Value="40" />
            <Setter Property="Cursor" Value="Hand" />
            <Setter Property="Foreground" Value="{DynamicResource TextBrush}" />
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="Root" CornerRadius="12" Padding="18,0"
                                Background="{DynamicResource SoftButtonBackgroundBrush}"
                                BorderBrush="{DynamicResource SoftButtonBorderBrush}" BorderThickness="1">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center" />
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="Root" Property="Background" Value="{DynamicResource SoftButtonHoverBrush}" />
                                <Setter TargetName="Root" Property="BorderBrush" Value="{DynamicResource AccentBrush}" />
                            </Trigger>
                            <Trigger Property="IsEnabled" Value="False">
                                <Setter TargetName="Root" Property="Opacity" Value="0.45" />
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <!-- Крестик заголовка: без рамки и заливки, аккуратный символ ✕ и красная подсветка. -->
        <Style x:Key="TitleCloseButton" TargetType="Button">
            <Setter Property="Width" Value="34" />
            <Setter Property="Height" Value="30" />
            <Setter Property="Cursor" Value="Hand" />
            <Setter Property="FontSize" Value="15" />
            <Setter Property="Foreground" Value="{DynamicResource MutedBrush}" />
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="Root" CornerRadius="8" Background="Transparent">
                            <TextBlock Text="&#x2715;" HorizontalAlignment="Center" VerticalAlignment="Center"
                                       Foreground="{TemplateBinding Foreground}" FontSize="{TemplateBinding FontSize}" />
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="Root" Property="Background" Value="#E03B4A" />
                                <Setter Property="Foreground" Value="White" />
                            </Trigger>
                            <Trigger Property="IsPressed" Value="True">
                                <Setter TargetName="Root" Property="Background" Value="#B62C39" />
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style x:Key="AccentButton" TargetType="Button" BasedOn="{StaticResource FlatButton}">
            <Setter Property="FontWeight" Value="Bold" />
            <Setter Property="Foreground" Value="#101815" />
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="Root" CornerRadius="12" Padding="18,0"
                                Background="{DynamicResource AccentBrush}"
                                BorderBrush="{DynamicResource AccentBrush}" BorderThickness="1">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center" />
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="Root" Property="Opacity" Value="0.9" />
                            </Trigger>
                            <Trigger Property="IsEnabled" Value="False">
                                <Setter TargetName="Root" Property="Opacity" Value="0.45" />
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </Window.Resources>

    <Border BorderBrush="{DynamicResource TitleBarBorderBrush}" BorderThickness="1">
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto" />
                <RowDefinition Height="Auto" />
                <RowDefinition Height="*" />
                <RowDefinition Height="Auto" />
            </Grid.RowDefinitions>

            <Border Grid.Row="0" Height="46" Background="{DynamicResource TitleBarBackgroundBrush}"
                    BorderBrush="{DynamicResource TitleBarBorderBrush}" BorderThickness="0,0,0,1"
                    MouseLeftButtonDown="TitleBar_MouseLeftButtonDown">
                <Grid Margin="14,0,10,0">
                    <TextBlock Text="Дополнительные моды" VerticalAlignment="Center"
                               FontSize="15" FontWeight="SemiBold" Foreground="{DynamicResource TextBrush}" />
                    <Button HorizontalAlignment="Right" Style="{StaticResource TitleCloseButton}"
                            Click="CloseButton_Click" ToolTip="Закрыть" />
                </Grid>
            </Border>

            <StackPanel Grid.Row="1" Margin="22,18,22,10">
                <TextBlock Text="Моды, одобренные администрацией" FontSize="18" FontWeight="Bold"
                           Foreground="{DynamicResource TextBrush}" />
                <TextBlock x:Name="SubtitleTextBlock" Margin="0,6,0,0" TextWrapping="Wrap" FontSize="12"
                           Foreground="{DynamicResource MutedBrush}"
                           Text="Отметь моды, которые хочешь добавить к сборке. Они переустановятся автоматически после каждого обновления." />
            </StackPanel>

            <ScrollViewer x:Name="ModsScrollViewer" Grid.Row="2" Margin="22,0,22,0"
                          VerticalScrollBarVisibility="Auto" HorizontalScrollBarVisibility="Disabled"
                          PreviewMouseWheel="ModsScrollViewer_PreviewMouseWheel">
                <StackPanel>
                    <TextBlock x:Name="EmptyTextBlock" Visibility="Collapsed" Margin="2,10,0,0"
                               TextWrapping="Wrap" FontSize="12" Foreground="{DynamicResource MutedBrush}"
                               Text="Для этой сборки пока нет одобренных дополнительных модов." />

                    <ItemsControl x:Name="ModsPanel">
                        <ItemsControl.ItemTemplate>
                            <DataTemplate>
                                <Border Margin="0,0,0,10" CornerRadius="14" Padding="14,12"
                                        Background="{DynamicResource CardAltBrush}"
                                        BorderBrush="{DynamicResource StrokeBrush}" BorderThickness="1">
                                    <Grid>
                                        <Grid.ColumnDefinitions>
                                            <ColumnDefinition Width="Auto" />
                                            <ColumnDefinition Width="12" />
                                            <ColumnDefinition Width="*" />
                                            <ColumnDefinition Width="Auto" />
                                        </Grid.ColumnDefinitions>

                                        <CheckBox VerticalAlignment="Center" IsChecked="{Binding Selected, Mode=TwoWay}"
                                                  Foreground="{DynamicResource TextBrush}" />

                                        <StackPanel Grid.Column="2">
                                            <StackPanel Orientation="Horizontal">
                                                <TextBlock Text="{Binding Name}" FontSize="14" FontWeight="SemiBold"
                                                           Foreground="{DynamicResource TextBrush}" />
                                                <Border Margin="8,0,0,0" CornerRadius="7" Padding="7,2"
                                                        Background="{DynamicResource PanelAltBrush}"
                                                        Visibility="{Binding CategoryVisibility}">
                                                    <TextBlock Text="{Binding Category}" FontSize="10"
                                                               Foreground="{DynamicResource MutedBrush}" />
                                                </Border>
                                            </StackPanel>
                                            <TextBlock Text="{Binding Description}" Margin="0,4,0,0" FontSize="11"
                                                       TextWrapping="Wrap" Foreground="{DynamicResource MutedBrush}"
                                                       Visibility="{Binding DescriptionVisibility}" />
                                            <TextBlock Text="{Binding Meta}" Margin="0,4,0,0" FontSize="10"
                                                       Foreground="{DynamicResource MutedBrush}" />
                                        </StackPanel>
                                    </Grid>
                                </Border>
                            </DataTemplate>
                        </ItemsControl.ItemTemplate>
                    </ItemsControl>
                </StackPanel>
            </ScrollViewer>

            <Grid Grid.Row="3" Margin="22,14,22,18">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*" />
                    <ColumnDefinition Width="Auto" />
                    <ColumnDefinition Width="10" />
                    <ColumnDefinition Width="Auto" />
                </Grid.ColumnDefinitions>

                <TextBlock x:Name="StatusTextBlock" VerticalAlignment="Center" FontSize="12"
                           TextWrapping="Wrap" Foreground="{DynamicResource MutedBrush}" Text="" />
                <Button Grid.Column="1" Width="120" Style="{StaticResource FlatButton}" Click="CancelButton_Click" Content="Отмена" />
                <Button x:Name="ApplyButton" Grid.Column="3" Width="180" Style="{StaticResource AccentButton}"
                        Click="ApplyButton_Click" Content="Применить" />
            </Grid>
        </Grid>
    </Border>
</Window>
